

## P0 (design decisions — surface, do not auto-fix)
- **Pattern 9 — `e.final_cohort.df = final_cohort` reassignment**: Directly assigning to `ExtractItem.df` is a contract question — ExtractItem typically owns its `.df` via `create_extract`/`entityExtract`. If direct `.df` assignment is supported by lhn, document it; if not, replace the example with the canonical way to wrap an arbitrary DataFrame in an ExtractItem for `.to_csv()` export (or show writing CSV directly via Spark). Decide and document, since this contradicts the "use the package method" principle promoted in §2.
- **Pattern 5 — return-value vocabularies**: The comments enumerate exact output strings (`'Hispanic'`, `'Black'`, `'Q1'`, etc.). These are claims about lhn's grouping functions that the harness does not validate. Confirm they match the current `group_*` / `assign_age_group` implementations or replace with a pointer to the function docstrings as the source of truth.
- **Pattern 2 Step 4 — gap-segmentation config keys** (`datefieldStop`, `max_gap`, `sort_fields`, output `course_of_therapy`, `therapy_gaps`): These are presented as authoritative config property names but are not cross-checkable from the harness gate. Confirm against the lhn `write_index_table` implementation / config schema or mark as illustrative.

## P1 (inaccuracies, contradictions, missing critical facts)
- **§"Notebook Authoring Conventions" intro / §3 / Standard setup — cwd contradiction**: Text says `pipeline_setup` "derives the project from the notebook's cwd — run from `Projects/<project>`", but the `hdl_run.py` example passes `--hdl-project-dir "$HOME/work/Users/$USER/Projects/derm"` and the harness is invoked locally from an arbitrary cwd. Clarify that cwd-derivation applies to **notebook execution on HDL** (where nbconvert runs in `Projects/<project>`), not to the local `check_notebook.py` invocation. Suggested fix: add a sentence "On HDL, nbconvert executes the notebook from `Projects/<project>`; the local gate does not require this cwd."
- **§"Notebook Authoring Conventions" intro — gate invocation example is redundant with §7**: The standalone `hdl_run.py ... --validate` block here shows only `--validate`, while §7 promotes `--all --fix-loop`. Replace the intro block with a one-liner pointing to §7, or explicitly label this as "gate-only (no deploy)" so readers don't think `--validate` alone is the recommended pre-ship command.
- **§6 txtarchive — raw cell triple-quote claim**: States the raw cell must be wrapped in triple quotes and that "without the triple-quote wrapper the YAML is lost on extraction." Confirm this matches current `txtarchive` parser behavior; some versions accept a bare raw cell with `---` fences. If both are accepted, soften to "triple-quote wrapping is the supported convention for LLM round-tripping"; if strictly required, keep but add a pointer to the txtarchive parser source/section that enforces it.
- **§7 — `--all` vs `--execute` claim**: "a separate `--execute` flag is unnecessary" — confirm `hdl_run.py` does not actually expose `--execute` as a distinct flag. If it does (even as an alias), the sentence is misleading; reword to "`--all` already includes execution; do not pass `--execute` separately" or remove.
- **§7 — `--fix-loop` description**: Says "cycle check → 3090 fix → re-check". The "3090" is opaque jargon (presumably the GPU fix model/host). Either name it explicitly (e.g., "local LLM fixer running on the 3090 workstation") or drop the number and say "automated fixer".
- **Pattern 1 Step 2 — `showIU` comment**: Comment says "(IU tenant filter)" but `showIU` is not defined or referenced anywhere else in the doc, and the name suggests a specific tenant abbreviation. Either (a) add a one-line definition the first time it appears ("`showIU` = `show` filtered to the IU tenant for PHI-safe preview") or (b) replace with the generic `e.X.show(obs=10)` to avoid introducing an undocumented method.
- **Pattern 1 Step 4 — `filterSimple` kwarg**: Listed among `write_index_table` optional kwargs but never explained. Either add a one-sentence description or remove it from the enumeration to avoid implying it's commonly used.
- **Pattern 3 Step 2 — `r.labsmallSource` vs `r.labSource`**: Two distinct refs are used (small dictionary vs full source) without explanation. Add a one-line note: "`r.labsmallSource` is the dictionary/lookup; `r.labSource` is the full result table" so readers don't assume a typo.
- **Pattern 6 — `e.dx_index.write_index_table(inTable=e.dx_conditions)` with no date window**: Inconsistent with Patterns 1/2 which always pass `histStart`/`histEnd`. Either add the kwargs for consistency or add a comment that `histStart`/`histEnd` fall back to config defaults if omitted (and confirm that's true).
- **Pattern 9 — `writeTable` `outTable=f'{ctx.projectSchema}.final_cohort_scd_rwd'`**: Confirm the context attribute is `projectSchema` (camelCase mixed with `dataLoc` is plausible but should match the actual `pipeline_setup` return). If the attribute is `project_schema` or `schema`, fix; otherwise leave but cross-link to the bootstrap doc.
- **Pattern 4 — `calcUsage` `fields=` argument**: Includes `'personid'` and two date columns but the function then takes `index=['personid']` and `dateCoalesce=...` separately. Confirm `fields=` is still a required arg in current `calcUsage` (it looks vestigial). If not, remove; if yes, add a one-line comment on its purpose.
- **§"Common Field Names Reference" — `conditioncode_standard_id` vs `procedurecode_standard_id`**: The §1 GOOD example uses `procedurecode_standard_id` from `r.procedureSource`, but the reference table only documents condition/medication/lab fields, not procedure fields. Add a "Procedure Fields" subsection (or note that procedure fields follow the same `<entity>code_standard_id` / `_display` pattern) so the §1 example is grounded.

## P2 (clarity, style, cross-links, minor gaps)
- **§"Notebook Authoring Conventions" §2 table — `load_csv_as_df()`**: Add an inline link to the projectTables `csv:` config schema (or §Pattern 9 / 000-control.yaml docs) so readers know where to declare the path.
- **§4 — API reference path**: `~/projects/hdl-harness/docs/api_reference.md` appears three times. Define it once as a variable/link at the top of "Notebook Authoring Conventions" and reference it.
- **§7 — HDL render path diagram**: The arrow sequence `nbconvert --execute → quarto --no-execute (md or html) → Projects/archive/<subdir>/ → git push → local git pull → PHI scan` would read better as a numbered list or mermaid `flowchart LR`. Optional but improves scan-ability.
- **Pattern 1 Step 4 — output column naming**: Good explanation, but add a cross-link from Pattern 6 Step 3 comment back to Pattern 1 Step 4 instead of restating the rule (currently duplicated).
- **Pattern 5 — function imports**: `from lhn import (group_ethnicities, ...)` — confirm these are top-level lhn exports vs `lhn.demographics.*`. If submodule, fix import path.
- **Pattern 8 — `count_people` import**: Shown as `from lhn import count_people` but the function call uses keyword args (`description=`, `person_id=`). Add a one-line signature note or link to API ref.
- **Pattern 9 — three export idioms shown** (`writeTable`, `to_csv`, raw `.write.parquet`): Add a one-line "when to use which" guideline (e.g., "`writeTable` for Spark catalog tables; `to_csv` for archive deliverables; parquet for downstream re-load").
- **§Performance Tips #5 — `.limit(1000)` removal**: Cross-link to §7 (`hdl_run.py --all`) and to a gate rule, if `check_notebook.py` lints for stray `.limit(`. If it does, mention that explicitly; if not, the tip belongs in a "Pre-flight checklist" rather than performance.
- **Top of file** — add a one-line "Last validated against lhn vX.Y / hdl-harness vA.B" stamp so readers know how fresh the patterns are relative to the live API.
- **§Error handling — Pattern 8 back-link**: `(Pattern 8)` should be a markdown link `[Pattern 8](#pattern-8-attrition-tracking)` matching the bidirectional cross-link style used elsewhere.