

## P0 (design decisions — surface, do not auto-fix)
- §"HDL deploy, execute, render, and review" / `--all` description: the doc claims `hdl_run.py --all` triggers `render-and-push.sh` "via the automation layer (no manual SSH step when using the orchestrator)." This is a real workflow claim that needs confirmation — if `--all` actually requires a separate HDL-side trigger (the canonical sequence in the same section shows `fetchupdate → nbconvert → quarto → archive push` as discrete steps), the doc is overstating orchestrator scope. Confirm whether the local orchestrator can in fact initiate the HDL-side render, or whether the user must still run `render-and-push.sh` on HDL after `fetchupdate.sh`. Decide and align both paragraphs.
- §"Notebook Authoring Conventions" intro mandates "use the package method, and confirm column names from the data dictionary up front — don't write defensive code for unknowns." Pattern 5 (Demographics Standardization) directly references raw columns `ethnicity`, `race`, `gender`, `marital_status`, `age` against `r.demoSource.df` without a "confirm from catalog" callout, and the Common Field Names section lists `maritalstatus` (no underscore) as the raw field — contradicting the Pattern 5 example which uses `marital_status`. Decide which is canonical for the catalog and propagate.
- Pattern 9 shows `e.final_cohort.df = final_cohort` (direct attribute assignment to an ExtractItem's `.df`). This is the kind of "bypass the method" pattern §2 warns against. Decide whether assigning `.df` is a sanctioned ExtractItem contract or whether there's a setter/loader (`set_df`, `from_df`, etc.) that should be used; if direct assignment is allowed, say so explicitly to pre-empt gate complaints.

## P1 (inaccuracies, contradictions, missing critical facts)
- §"Standard notebook setup" / §3: code imports `Window` from `lhn.header` but no pattern uses it. Either drop `Window` from the canonical import (keep the block minimal/lint-clean) or add a note that it's included for window-function patterns. Suggested fix: remove `, Window` unless a downstream pattern uses it.
- Pattern 1 Step 2 comment says `showIU(obs=10)   # sample matched codes (IU tenant filter)`. `IU` here is ambiguous — readers will not know whether `IU` is a tenant code, an institution, or a method-naming convention. Fix: spell it out, e.g., `# show with IU-tenant filter applied (Indiana University tenant)` or rename if `showIU` is actually a generic "show with intermediate-uniqueness filter".
- Pattern 2 Step 4 lists `sort_fields: [administrationdate]` as a config property and `datefieldStop: administrationenddate`, but the Common Field Names → Medication Fields section uses the same names. Confirm these are the catalog spellings (no underscores) — and if `write_index_table`'s gap-segmentation config keys are actually `datefieldStop` / `max_gap` / `sort_fields` (mixed camel/snake), state that explicitly the way §4 does for API kwargs. Suggested fix: add a one-line note "config keys mirror the API's camel/snake mix; see api_reference.md."
- §"Notebook Authoring Conventions" intro shows `python $HARNESS/hdl_run.py <notebook>.txt --validate` but the §7 orchestrator example uses `--all --fix-loop` with `--catalog $CATALOG`, `--transfer`, `--hdl-project-dir`. The intro snippet omits `--transfer` and `--hdl-project-dir` — for `--validate` alone these may be optional, but say so. Suggested fix: add "for `--validate` only, `--transfer` / `--hdl-project-dir` are not required."
- §6 (txtarchive): "The raw cell must be **cell 1**" and "`---` YAML fences inside the triple quotes are required." The skeleton example shows `# Raw Cell` (no number) while code/markdown cells are `# Cell N` / `# Markdown Cell N`. Clarify whether the raw cell header is literally `# Raw Cell` or `# Raw Cell 1`. Suggested fix: pin the exact header string txtarchive expects.
- §7 bullet on md vs html: "**md** when the executed notebook has no graphics or formatted tables (gtsummary/gt HTML). **html** when it does (`render_format.py` auto-detects)." Confirm `render_format.py` is the actual script name on HDL (vs e.g. `render-and-push.sh` choosing format). Fix: name the exact detector and where it lives, or link to txtarchive-hdl-integration.md for the rule.
- Pattern 4 `calcUsage` call passes both `fields=[..., 'actualarrivaldate', 'servicedate']` and `dateCoalesce=F.coalesce(...)`. Confirm whether `fields=` must include the date columns when `dateCoalesce` is supplied, or whether `dateCoalesce` alone suffices. Add a one-line comment so readers don't double-specify by accident.
- Pattern 8 imports `count_people` from `lhn` top-level, while Pattern 5/7 import from `lhn` and `lhn.header` respectively. Confirm `count_people` and `attrition` are re-exported at `lhn` top level; if not, fix the import path. Same check for `aggregate_fields`, `calcUsage`, `writeTable`, `group_*`, `assign_age_group` — list the authoritative import surface once at the top of patterns to prevent gate-flagged imports.
- §"Performance Tips" #2 says "Use `broadcast_flag=True` on `entityExtract` for small lookup tables." §4 lists `broadcast_flag` as snake_case in the camel/snake mix. Confirm this is the actual kwarg name on `entityExtract` (not `broadcast`, `broadcastFlag`, etc.) — the gate will reject a wrong spelling. Either verify against `api_reference.md` and keep, or correct.
- Pattern 1 Step 4 comment: "Output column names default to `index_<code>` and `last_<code>` from the ExtractItem's `code` property (here: `index_diabetes`, `last_diabetes`)." Pattern 2 then says "Result includes: `course_of_therapy`, `therapy_gaps`, etc." but does not show the analogous `index_hydroxyurea` / `last_hydroxyurea` columns. Either show the full output column list for Pattern 2, or note that gap-mode adds `course_of_therapy`/`therapy_gaps` *in addition to* the base `index_<code>`/`last_<code>`. Avoids reader confusion that gap mode replaces the default columns.
- §"Legacy note" says "migrate inherited notebooks before running the gate" — does `check_notebook.py` actually flag `Resources(local_config=...)` as a gate failure, or just warn? State the gate behavior so the migration urgency is calibrated.

## P2 (clarity, style, cross-links, minor gaps)
- §"Three-Step Extraction Pattern" overview omits a `create_extract` → `entityExtract` distinction blurb. Add one line: "`create_extract` searches a *reference/dictionary* table for codes; `entityExtract` pulls *patient records* for those codes." Prevents the common mix-up.
- §"Common Field Names Reference" — add a sentence pointing to the harness command that prints the exact column list for a given ref (e.g., `python $HARNESS/check_notebook.py --describe r.conditionSource`) if such a flag exists, or note the catalog JSON path so readers can grep it directly.
- Pattern 3 mixes `from lhn.header import F` convention (used in Pattern 7) with `import pyspark.sql.functions as F` (used in Patterns 3 and 4). Pick one and apply consistently — `from lhn.header import F` matches the §"Standard notebook setup" block.
- Pattern 5 listed return values for `group_races` — `'Indigenous'` is one common spelling but verify against the function's actual output (some implementations use `'American Indian/Alaska Native'` or `'AIAN'`). Confirm or correct.
- Pattern 6 uses `e.dx_cohort.create_extract(...)` / `e.dx_conditions.entityExtract(...)` with ellipses — for a "canonical pattern guide" this is thin. Either inline a minimal but real example, or add `# see Pattern 1 Steps 2–3 for full kwargs` cross-links on each line.
- Pattern 9: `e.final_cohort.to_csv()` and `e.final_cohort.df.write.parquet(e.final_cohort.parquet)` — clarify that `.csv` and `.parquet` are ExtractItem path properties resolved from `000-control.yaml projectTables`. Add a one-line comment matching the §2 table row that introduced `load_csv_as_df`.
- §"Notebook Authoring Conventions" → rule 5 ("No hardcoded absolute paths") would benefit from a GOOD/BAD code pair like rules 1, 2, 4 — currently it's prose-only.
- §"Performance Tips" #5: "remove any `.limit(...)` before `hdl_run.py --all`" — note whether the gate (`check_notebook.py`) flags `.limit(` calls, since that would make the rule mechanically enforced rather than a tip. If it does, promote this to a §"Notebook Authoring Conventions" rule.
- §7 cross-link: the inline reference to `~/projects/hdl-harness/docs/txtarchive-hdl-integration.md` appears twice (in §6 and §7 area). Consider consolidating into a single "Further reading" footer block with all paths (`api_reference.md`, `txtarchive-hdl-integration.md`, `create-archive-llm-instructions.md`, `hdl_catalog.json`).
- Pattern 7 comment "column is index_<code> from write_index_table — here code: cohort" — make consistent with Pattern 1's `# code: diabetes` style (use a config snippet block, not inline prose) for visual parity across patterns.