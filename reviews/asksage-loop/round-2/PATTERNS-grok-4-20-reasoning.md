

**P0 (design decisions — surface, do not auto-fix)**
- **Pattern 7: Time-Window Analysis (full section)**: Hand-rolls a join + `date_sub`/`date_add` filter instead of using an `ExtractItem` method (contradicts the “Use lhn methods — don’t hand-roll” rule in “Notebook Authoring Conventions #2”). Surface whether a canonical method (e.g. `extract_window_around_index` or similar on `ExtractItem`) should be added to the package rather than encouraging per-notebook Spark code. Do not auto-fix.
- **Overall document / ExtractItem contract**: Heavy emphasis that `write_index_table`, gap logic, column prefixes, etc. are driven exclusively by `000-control.yaml` `projectTables` config (not call-site kwargs). This is a deliberate design choice vs. a more explicit Python API. Surface for confirmation against current `hdl-harness` gate rules and `ExtractItem` implementation. Do not auto-fix.

**P1 (inaccuracies, contradictions, missing critical facts)**
- **Pattern 2, YAML config comment for `hydroxyurea_index`**: Inconsistent config key casing (`indexFields`, `datefieldPrimary`, `datefieldStop` are camelCase; `max_gap` and `sort_fields` are snake_case). The gate and `pipeline_setup` expect a single consistent contract. **Exact suggested fix**: Standardize all keys to the canonical form shown in `api_reference.md` (most likely `maxGap: 90` and `sortFields: [administrationdate]` or the exact spelling used by the bootstrap). Update the comment block to match.
- **Common Field Names Reference → Demographics Fields**: Lists raw column as ``maritalstatus`` but Pattern 5 code block passes ``'marital_status'`` to `group_marital_status`. Column validation in `check_notebook.py` and the catalog will fail or produce silent mismatches. **Exact suggested fix**: Replace with the exact column name resolved from `hdl_catalog.json` (via harness). Change the Pattern 5 call to `group_marital_status(demo, 'maritalstatus', 'marital_std')` (or correct the reference table if the catalog actually uses the underscore form). Add note: “Confirm exact name with `hdl_run.py --validate`”.
- **Pattern 3, Step 3 (`aggregate_fields` call)**: Comment explicitly states “(`fields` is unused in the API; groupby is inferred)”. This contradicts the “use the package method … confirm … from the data dictionary” rule and makes the example misleading for gate linting. **Exact suggested fix**: Remove the `fields=` argument if truly unused, or update the call and delete the parenthetical comment so it matches the live signature in `api_reference.md`. Re-validate with `hdl_run.py --validate`.
- **Pattern 9, `writeTable` + `to_csv` usage**: Uses `ctx.projectSchema` but the “Standard notebook setup” block only unpacks `dataLoc = ctx.dataLoc`. New readers following the standard setup literally will get an undefined `ctx` reference. **Exact suggested fix**: Add `projectSchema = ctx.projectSchema` (or `schema = ctx.projectSchema`) to the standard setup code block that appears twice in the document.

**P2 (clarity, style, cross-links, minor gaps)**
- **“Notebook Authoring Conventions #6” (txtarchive section)**: Raw-cell YAML example uses kernel `pyspark-lhn-dev`. The orchestrator examples later use varying project directories (`allison/`, `derm`). **Exact suggested fix**: Add cross-link to `~/projects/hdl-harness/docs/txtarchive-hdl-integration.md` at the end of the minimal header skeleton and note “Kernel and project directory must match the `--hdl-project-dir` and kernel used in `hdl_run.py --all`”.
- **Performance Tips #5**: Says “remove any `.limit(...)` before `hdl_run.py --all`”. The document never shows a `.limit()` in any pattern, creating a minor gap. **Exact suggested fix**: Add a one-line example in Pattern 1 Step 2: `e.diabetes_codes.showIU(obs=10)` → `e.diabetes_codes.showIU(obs=10).limit(1000)` with a comment “Remove limit before final `--all --fix-loop` run”.
- **Pattern 8 → link to error handling**: Anchor `#error-handling-on-hdl-fail-loud-dont-hedge` is correct but the heading uses parentheses. Some markdown renderers produce slightly different slugs. **Exact suggested fix**: Change link to `[Error handling on HDL (fail loud, don't hedge)](#error-handling-on-hdl-fail-loud-dont-hedge)` (include parentheses in link text) for robustness.
- **Dates throughout (Patterns 1, 2, 4, 7)**: Hard-coded `histEnd='2025-01-28'` while today is 2026-06-26. **Exact suggested fix**: Change all example `histEnd` values to `'2026-12-31'` (or `histEnd=ctx.maxDate` if supported) and add “Use dates appropriate for your study period; confirm against catalog”.

**CONVERGED: no changes recommended**