

## P0 (design decisions — surface, do not auto-fix)
- **Pattern 9, "Export to CSV / Parquet" block**: Reassigning `e.final_cohort.df = final_cohort` then calling `e.final_cohort.to_csv()` / `.write.parquet(e.final_cohort.parquet)` bypasses the ExtractItem extraction lifecycle and may not be the documented sink path. Decide whether the canonical export is (a) `writeTable(...)` only, (b) an ExtractItem method like `to_csv()` invoked on an item *populated by extraction*, or (c) explicit `DF.write.*` against `ctx.dataLoc`-derived paths. Currently the doc shows three different mechanisms in seven lines with no guidance on which the harness/standards lint accepts.
- **Pattern 3, `aggregate_fields` signature**: The inline comment says "`fields` is unused in the API; groupby is inferred" yet the call still passes `fields=['resultvalue']`. Decide whether to (a) drop `fields=` from the example entirely, or (b) keep it and remove the contradictory comment. Showing a deprecated/no-op kwarg in the canonical pattern guide will get copy-pasted forever.
- **Section 7 "HDL deploy…" vs Section "Notebook Authoring Conventions" intro**: Both describe the orchestrator invocation but with different flag sets (`--validate` vs `--all --fix-loop`) and different example notebooks. Decide on a single canonical invocation block and have the other section cross-link to it, rather than maintaining two near-duplicates that will drift.

## P1 (inaccuracies, contradictions, missing critical facts)
- **Section 3 "Bootstrap" and "Standard notebook setup"**: Both blocks are byte-identical and appear within ~40 lines of each other. Collapse to one; have the convention section reference the "Standard notebook setup" anchor. Currently any edit to one will silently desynchronize the other.
- **Section 4 "Use the real API"**: Lists `find_method` and `broadcast_flag` as "generated API casing" examples alongside `elementList`/`cacheResult`, but those two are snake_case while the rule being illustrated is camelCase. Either (a) state explicitly that the API mixes casings and these are the authoritative spellings, or (b) move `find_method`/`broadcast_flag` to a separate "note: these are intentionally snake_case" line so readers don't infer a typo.
- **Pattern 1, Step 4 config snippet**: Comment says "NOTE: config property is `indexFields`" but Pattern 2's YAML block at Step 1 uses `indexFields: [drugcode]` for `hydroxyurea_codes` — which is a *code* table, not an index table. `indexFields` on a code-lookup ExtractItem is almost certainly wrong (codes don't have a person grain). Fix Pattern 2 Step 1 to omit `indexFields` for `hydroxyurea_codes` (or show the correct property for code tables).
- **Pattern 6, Step 3**: Joining `e.dx_index.df` and `e.rx_index.df` on `personid` while filtering on `index_diagnosis < index_medication` — but the preceding comment only says to set `code: diagnosis` / `code: medication`. The output column from `write_index_table` is `index_<code>`, so this works, but the doc should explicitly state the join produces `index_diagnosis`, `last_diagnosis`, `index_medication`, `last_medication` so readers don't go hunting for an `index_date` column.
- **Pattern 7**: Uses `e.cohort_index.df.select('personid', 'index_date')` — but per Pattern 1/6, `write_index_table` produces `index_<code>`, never a bare `index_date`. Either (a) show an explicit rename `.withColumnRenamed('index_diabetes', 'index_date')`, or (b) note that `index_date` is illustrative and the real column is `index_<code>`. As written this contradicts the rest of the guide.
- **Section 6 "txtarchive `.txt` format"**: The Raw Cell skeleton shows `'''` (triple single-quote) wrapper but does not state that this MUST be the *first* cell and that the `---/---` YAML fence inside the triple quotes is required for `extract-notebooks` to populate the notebook metadata. Add: "The raw cell must be cell 1; the `---` fences inside the triple quotes are required — they are what `txtarchive` parses for `title`/`jupyter.kernelspec`."
- **Section 7, render path diagram**: Shows `nbconvert --execute → quarto --no-execute (md or html) → Projects/archive/<subdir>/ → git push → local git pull`. Missing the PHI scan step that the prose later mentions ("PHI scan on --pull still runs"). Insert `→ PHI scan` between `local git pull` and the end, so the diagram matches the orchestrator stages.
- **Section "Standard notebook setup" Legacy note**: Says "The harness rejects `Resources()`" — confirm this is a hard reject (gate failure) vs a warning. If it's a warning, soften the wording; if hard-reject, add the exact gate rule name (e.g., `bootstrap-uses-pipeline-setup`) so authors can grep the harness output.
- **Pattern 8, `count_people` import**: Imports `attrition, count_people` from `lhn` top-level but Pattern 7 imports `F` from `lhn.header`. Confirm `count_people` and `attrition` are re-exported from `lhn` top-level (vs `lhn.header` or `lhn.utils`). If not, fix the import; if yes, add a one-line note that aggregation/utility helpers live at `lhn.*` while Spark shims live at `lhn.header.*`.

## P2 (clarity, style, cross-links, minor gaps)
- **Section 2 table**: The row "`spark.read.csv(path)` → `e.X.load_csv_as_df()` (csv path comes from config)" doesn't specify *which* config key supplies the CSV path. Add: "(reads from the ExtractItem's `csv:` property in `000-control.yaml projectTables`)".
- **Pattern 1 Step 2**: `e.diabetes_codes.showIU(obs=10)` is the only appearance of `showIU` in the document. Add a one-line comment explaining what `showIU` shows ("intermediate unique values — the codes matched") or link to the API ref. New readers will not know what IU stands for.
- **Pattern 5**: `assign_age_group(demo, 'age')` — clarify whether `age` is expected to be pre-computed numeric, or whether the function computes it from `birthdate`. Currently ambiguous given the Demographics field list shows `birthdate` but no `age`.
- **"Common Field Names Reference"** intro: Good that it points to `hdl_catalog.json`, but add the exact harness command to dump catalog columns for a single ref, e.g. `python $HARNESS/check_notebook.py --catalog-dump r.conditionSource`, so readers don't have to grep JSON.
- **Performance Tips #2**: "Use `broadcast_flag=True` on `entityExtract`" — specify the size threshold above which this becomes counter-productive (typical Spark guidance: <10MB / <~1M rows). Otherwise readers will broadcast everything.
- **Pattern 4, `calcUsage`**: `fields=['personid', 'actualarrivaldate', 'servicedate']` overlaps with `index=['personid']` and `dateCoalesce` already references the two date columns. Add a one-line comment on what `fields=` controls (selection? grouping?) — currently it looks redundant.
- **Cross-link**: Section "Notebook Authoring Conventions" item 7 references `~/projects/hdl-harness/docs/txtarchive-hdl-integration.md` and item 4 references `~/projects/hdl-harness/docs/api_reference.md` — add a single "Reference docs" subsection at the top of the conventions block listing all four external paths (`api_reference.md`, `txtarchive-hdl-integration.md`, `create-archive-llm-instructions.md`, `hdl_catalog.json`) so they are discoverable without scanning seven subsections.
- **Pattern 9 comment**: "schema from pipeline_setup context" — show the actual attribute (`ctx.projectSchema`) is used in the f-string, which is good, but add that `partitionBy='tenant'` requires `tenant` to exist in `final_cohort` (it doesn't appear in the Pattern 6 join `select`). Either add `tenant` to the demographics select in Pattern 6 or note the prerequisite here.